{
  "id": 2478,
  "title": "SQL Beginner Concepts - Module #2478",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `nodes` in table `cache`?",
  "options": [
    "SELECT DISTINCT nodes FROM cache;",
    "SELECT UNIQUE nodes FROM cache;",
    "SELECT DIFFERENT nodes FROM cache;",
    "SELECT INDIVIDUAL nodes FROM cache;"
  ],
  "answer": "SELECT DISTINCT nodes FROM cache;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}