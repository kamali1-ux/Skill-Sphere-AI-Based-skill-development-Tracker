{
  "id": 2530,
  "title": "SQL Beginner Concepts - Module #2530",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `payload` in table `stream`?",
  "options": [
    "SELECT DIFFERENT payload FROM stream;",
    "SELECT DISTINCT payload FROM stream;",
    "SELECT INDIVIDUAL payload FROM stream;",
    "SELECT UNIQUE payload FROM stream;"
  ],
  "answer": "SELECT DISTINCT payload FROM stream;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}