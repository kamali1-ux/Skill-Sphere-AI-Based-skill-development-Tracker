{
  "id": 2662,
  "title": "SQL Beginner Concepts - Module #2662",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `users` in table `stream`?",
  "options": [
    "SELECT DIFFERENT users FROM stream;",
    "SELECT INDIVIDUAL users FROM stream;",
    "SELECT UNIQUE users FROM stream;",
    "SELECT DISTINCT users FROM stream;"
  ],
  "answer": "SELECT DISTINCT users FROM stream;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}