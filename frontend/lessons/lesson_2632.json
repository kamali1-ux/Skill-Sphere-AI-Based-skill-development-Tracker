{
  "id": 2632,
  "title": "SQL Beginner Concepts - Module #2632",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `data` in table `cache`?",
  "options": [
    "SELECT INDIVIDUAL data FROM cache;",
    "SELECT UNIQUE data FROM cache;",
    "SELECT DISTINCT data FROM cache;",
    "SELECT DIFFERENT data FROM cache;"
  ],
  "answer": "SELECT DISTINCT data FROM cache;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}