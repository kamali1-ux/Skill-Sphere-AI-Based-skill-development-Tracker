{
  "id": 3487,
  "title": "SQL Advanced Concepts - Module #3487",
  "category": "Data Science",
  "difficulty": "Advanced",
  "question": "How does the SQL window function `ROW_NUMBER() OVER (PARTITION BY token ORDER BY values)` behave?",
  "options": [
    "It ranks rows but leaves gaps in sequential numbering if duplicate values exist in column `values`.",
    "It counts the total rows grouped by column `token`.",
    "It assigns a unique sequential integer to rows within each partition of `token`, ordered by `values`.",
    "It sorts the physical rows in table `token` using a clustered index."
  ],
  "answer": "It assigns a unique sequential integer to rows within each partition of `token`, ordered by `values`.",
  "explanation": "ROW_NUMBER() assigns a unique number starting at 1 for each row in a partition, resetting the count when the partition column changes."
}