{
  "id": 2305,
  "title": "SQL Beginner Concepts - Module #2305",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `cache` in table `stream`?",
  "options": [
    "SELECT DISTINCT cache FROM stream;",
    "SELECT INDIVIDUAL cache FROM stream;",
    "SELECT UNIQUE cache FROM stream;",
    "SELECT DIFFERENT cache FROM stream;"
  ],
  "answer": "SELECT DISTINCT cache FROM stream;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}