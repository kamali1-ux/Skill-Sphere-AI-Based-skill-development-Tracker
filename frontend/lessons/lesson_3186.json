{
  "id": 3186,
  "title": "SQL Intermediate Concepts - Module #3186",
  "category": "Data Science",
  "difficulty": "Intermediate",
  "question": "Which aggregate function should you use to find the average value of the column `config` in table `server`?",
  "options": [
    "MEAN(config)",
    "AVG(config)",
    "AVERAGE(config)",
    "SUM(config) / COUNT(*)"
  ],
  "answer": "AVG(config)",
  "explanation": "AVG() is the standard SQL aggregate function for calculating the average of numeric column values."
}