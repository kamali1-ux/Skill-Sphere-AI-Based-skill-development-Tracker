{
  "id": 3090,
  "title": "SQL Intermediate Concepts - Module #3090",
  "category": "Data Science",
  "difficulty": "Intermediate",
  "question": "Which aggregate function should you use to find the average value of the column `config` in table `cache`?",
  "options": [
    "AVERAGE(config)",
    "AVG(config)",
    "SUM(config) / COUNT(*)",
    "MEAN(config)"
  ],
  "answer": "AVG(config)",
  "explanation": "AVG() is the standard SQL aggregate function for calculating the average of numeric column values."
}