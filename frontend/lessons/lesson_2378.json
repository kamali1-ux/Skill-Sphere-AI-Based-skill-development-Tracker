{
  "id": 2378,
  "title": "SQL Beginner Concepts - Module #2378",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `records` in table `cache`?",
  "options": [
    "SELECT DIFFERENT records FROM cache;",
    "SELECT UNIQUE records FROM cache;",
    "SELECT DISTINCT records FROM cache;",
    "SELECT INDIVIDUAL records FROM cache;"
  ],
  "answer": "SELECT DISTINCT records FROM cache;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}