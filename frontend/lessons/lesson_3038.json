{
  "id": 3038,
  "title": "SQL Intermediate Concepts - Module #3038",
  "category": "Data Science",
  "difficulty": "Intermediate",
  "question": "What is the difference between an `INNER JOIN` and a `LEFT JOIN` on tables `items` and `response`?",
  "options": [
    "`INNER JOIN` returns all rows from both tables; `LEFT JOIN` returns only matching rows from `items`.",
    "`INNER JOIN` returns matching rows in both tables; `LEFT JOIN` returns all rows from `items` plus matching rows from `response`.",
    "`INNER JOIN` requires primary keys on column `items`; `LEFT JOIN` does not require keys.",
    "`INNER JOIN` merges rows horizontally; `LEFT JOIN` appends them vertically."
  ],
  "answer": "`INNER JOIN` returns matching rows in both tables; `LEFT JOIN` returns all rows from `items` plus matching rows from `response`.",
  "explanation": "INNER JOIN filters out non-matching rows, while LEFT JOIN preserves all rows from the left table, padding matching columns from the right table with NULL where no match exists."
}