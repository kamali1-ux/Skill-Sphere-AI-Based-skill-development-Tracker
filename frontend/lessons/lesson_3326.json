{
  "id": 3326,
  "title": "SQL Intermediate Concepts - Module #3326",
  "category": "Data Science",
  "difficulty": "Intermediate",
  "question": "Which aggregate function should you use to find the average value of the column `token` in table `result`?",
  "options": [
    "MEAN(token)",
    "AVG(token)",
    "AVERAGE(token)",
    "SUM(token) / COUNT(*)"
  ],
  "answer": "AVG(token)",
  "explanation": "AVG() is the standard SQL aggregate function for calculating the average of numeric column values."
}