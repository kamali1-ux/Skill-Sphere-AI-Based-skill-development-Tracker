{
  "id": 3057,
  "title": "SQL Intermediate Concepts - Module #3057",
  "category": "Data Science",
  "difficulty": "Intermediate",
  "question": "Which aggregate function should you use to find the average value of the column `token` in table `config`?",
  "options": [
    "AVG(token)",
    "AVERAGE(token)",
    "MEAN(token)",
    "SUM(token) / COUNT(*)"
  ],
  "answer": "AVG(token)",
  "explanation": "AVG() is the standard SQL aggregate function for calculating the average of numeric column values."
}