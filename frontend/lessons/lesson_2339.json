{
  "id": 2339,
  "title": "SQL Beginner Concepts - Module #2339",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `client` in table `stream`?",
  "options": [
    "SELECT UNIQUE client FROM stream;",
    "SELECT INDIVIDUAL client FROM stream;",
    "SELECT DISTINCT client FROM stream;",
    "SELECT DIFFERENT client FROM stream;"
  ],
  "answer": "SELECT DISTINCT client FROM stream;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}