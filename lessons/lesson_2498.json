{
  "id": 2498,
  "title": "SQL Beginner Concepts - Module #2498",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `users` in table `nodes`?",
  "options": [
    "SELECT INDIVIDUAL users FROM nodes;",
    "SELECT DIFFERENT users FROM nodes;",
    "SELECT DISTINCT users FROM nodes;",
    "SELECT UNIQUE users FROM nodes;"
  ],
  "answer": "SELECT DISTINCT users FROM nodes;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}