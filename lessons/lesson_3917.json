{
  "id": 3917,
  "title": "SQL Advanced Concepts - Module #3917",
  "category": "Data Science",
  "difficulty": "Advanced",
  "question": "How does the SQL window function `ROW_NUMBER() OVER (PARTITION BY response ORDER BY buffer)` behave?",
  "options": [
    "It counts the total rows grouped by column `response`.",
    "It sorts the physical rows in table `response` using a clustered index.",
    "It assigns a unique sequential integer to rows within each partition of `response`, ordered by `buffer`.",
    "It ranks rows but leaves gaps in sequential numbering if duplicate values exist in column `buffer`."
  ],
  "answer": "It assigns a unique sequential integer to rows within each partition of `response`, ordered by `buffer`.",
  "explanation": "ROW_NUMBER() assigns a unique number starting at 1 for each row in a partition, resetting the count when the partition column changes."
}