{
  "id": 2602,
  "title": "SQL Beginner Concepts - Module #2602",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `payload` in table `token`?",
  "options": [
    "SELECT DIFFERENT payload FROM token;",
    "SELECT INDIVIDUAL payload FROM token;",
    "SELECT DISTINCT payload FROM token;",
    "SELECT UNIQUE payload FROM token;"
  ],
  "answer": "SELECT DISTINCT payload FROM token;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}