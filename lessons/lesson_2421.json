{
  "id": 2421,
  "title": "SQL Beginner Concepts - Module #2421",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `users` in table `config`?",
  "options": [
    "SELECT DISTINCT users FROM config;",
    "SELECT INDIVIDUAL users FROM config;",
    "SELECT DIFFERENT users FROM config;",
    "SELECT UNIQUE users FROM config;"
  ],
  "answer": "SELECT DISTINCT users FROM config;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}