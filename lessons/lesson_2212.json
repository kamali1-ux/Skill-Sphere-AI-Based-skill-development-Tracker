{
  "id": 2212,
  "title": "SQL Beginner Concepts - Module #2212",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `output` in table `token`?",
  "options": [
    "SELECT UNIQUE output FROM token;",
    "SELECT INDIVIDUAL output FROM token;",
    "SELECT DIFFERENT output FROM token;",
    "SELECT DISTINCT output FROM token;"
  ],
  "answer": "SELECT DISTINCT output FROM token;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}