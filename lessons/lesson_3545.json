{
  "id": 3545,
  "title": "SQL Advanced Concepts - Module #3545",
  "category": "Data Science",
  "difficulty": "Advanced",
  "question": "How does the SQL window function `ROW_NUMBER() OVER (PARTITION BY records ORDER BY token)` behave?",
  "options": [
    "It sorts the physical rows in table `records` using a clustered index.",
    "It assigns a unique sequential integer to rows within each partition of `records`, ordered by `token`.",
    "It ranks rows but leaves gaps in sequential numbering if duplicate values exist in column `token`.",
    "It counts the total rows grouped by column `records`."
  ],
  "answer": "It assigns a unique sequential integer to rows within each partition of `records`, ordered by `token`.",
  "explanation": "ROW_NUMBER() assigns a unique number starting at 1 for each row in a partition, resetting the count when the partition column changes."
}