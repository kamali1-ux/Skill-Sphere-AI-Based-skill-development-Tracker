{
  "id": 2168,
  "title": "SQL Beginner Concepts - Module #2168",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `users` in table `payload`?",
  "options": [
    "SELECT DIFFERENT users FROM payload;",
    "SELECT DISTINCT users FROM payload;",
    "SELECT UNIQUE users FROM payload;",
    "SELECT INDIVIDUAL users FROM payload;"
  ],
  "answer": "SELECT DISTINCT users FROM payload;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}