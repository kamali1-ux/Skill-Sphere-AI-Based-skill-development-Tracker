{
  "id": 2678,
  "title": "SQL Beginner Concepts - Module #2678",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `client` in table `buffer`?",
  "options": [
    "SELECT INDIVIDUAL client FROM buffer;",
    "SELECT UNIQUE client FROM buffer;",
    "SELECT DISTINCT client FROM buffer;",
    "SELECT DIFFERENT client FROM buffer;"
  ],
  "answer": "SELECT DISTINCT client FROM buffer;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}