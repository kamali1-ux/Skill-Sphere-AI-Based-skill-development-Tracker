{
  "id": 2155,
  "title": "SQL Beginner Concepts - Module #2155",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `buffer` in table `token`?",
  "options": [
    "SELECT INDIVIDUAL buffer FROM token;",
    "SELECT DIFFERENT buffer FROM token;",
    "SELECT UNIQUE buffer FROM token;",
    "SELECT DISTINCT buffer FROM token;"
  ],
  "answer": "SELECT DISTINCT buffer FROM token;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}