{
  "id": 2452,
  "title": "SQL Beginner Concepts - Module #2452",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `result` in table `nodes`?",
  "options": [
    "SELECT DISTINCT result FROM nodes;",
    "SELECT INDIVIDUAL result FROM nodes;",
    "SELECT UNIQUE result FROM nodes;",
    "SELECT DIFFERENT result FROM nodes;"
  ],
  "answer": "SELECT DISTINCT result FROM nodes;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}