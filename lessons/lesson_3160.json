{
  "id": 3160,
  "title": "SQL Intermediate Concepts - Module #3160",
  "category": "Data Science",
  "difficulty": "Intermediate",
  "question": "What is the difference between an `INNER JOIN` and a `LEFT JOIN` on tables `result` and `data`?",
  "options": [
    "`INNER JOIN` merges rows horizontally; `LEFT JOIN` appends them vertically.",
    "`INNER JOIN` returns all rows from both tables; `LEFT JOIN` returns only matching rows from `result`.",
    "`INNER JOIN` returns matching rows in both tables; `LEFT JOIN` returns all rows from `result` plus matching rows from `data`.",
    "`INNER JOIN` requires primary keys on column `result`; `LEFT JOIN` does not require keys."
  ],
  "answer": "`INNER JOIN` returns matching rows in both tables; `LEFT JOIN` returns all rows from `result` plus matching rows from `data`.",
  "explanation": "INNER JOIN filters out non-matching rows, while LEFT JOIN preserves all rows from the left table, padding matching columns from the right table with NULL where no match exists."
}