{
  "id": 2328,
  "title": "SQL Beginner Concepts - Module #2328",
  "category": "Data Science",
  "difficulty": "Beginner",
  "question": "Which SQL statement is used to retrieve only unique values from the column `client` in table `cache`?",
  "options": [
    "SELECT DIFFERENT client FROM cache;",
    "SELECT INDIVIDUAL client FROM cache;",
    "SELECT UNIQUE client FROM cache;",
    "SELECT DISTINCT client FROM cache;"
  ],
  "answer": "SELECT DISTINCT client FROM cache;",
  "explanation": "The DISTINCT keyword is the standard SQL syntax to filter out duplicate rows and return unique values."
}