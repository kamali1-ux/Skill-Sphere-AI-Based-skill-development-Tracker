{
  "id": 2821,
  "title": "SQL Intermediate Concepts - Module #2821",
  "category": "Data Science",
  "difficulty": "Intermediate",
  "question": "Which aggregate function should you use to find the average value of the column `config` in table `request`?",
  "options": [
    "MEAN(config)",
    "AVERAGE(config)",
    "AVG(config)",
    "SUM(config) / COUNT(*)"
  ],
  "answer": "AVG(config)",
  "explanation": "AVG() is the standard SQL aggregate function for calculating the average of numeric column values."
}