{
  "id": 2893,
  "title": "SQL Intermediate Concepts - Module #2893",
  "category": "Data Science",
  "difficulty": "Intermediate",
  "question": "What is the difference between an `INNER JOIN` and a `LEFT JOIN` on tables `server` and `output`?",
  "options": [
    "`INNER JOIN` requires primary keys on column `server`; `LEFT JOIN` does not require keys.",
    "`INNER JOIN` returns matching rows in both tables; `LEFT JOIN` returns all rows from `server` plus matching rows from `output`.",
    "`INNER JOIN` merges rows horizontally; `LEFT JOIN` appends them vertically.",
    "`INNER JOIN` returns all rows from both tables; `LEFT JOIN` returns only matching rows from `server`."
  ],
  "answer": "`INNER JOIN` returns matching rows in both tables; `LEFT JOIN` returns all rows from `server` plus matching rows from `output`.",
  "explanation": "INNER JOIN filters out non-matching rows, while LEFT JOIN preserves all rows from the left table, padding matching columns from the right table with NULL where no match exists."
}